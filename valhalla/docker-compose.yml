services:
  valhalla:
    init: true
    privileged: true
    restart: always
    build:
      context: .
    volumes:
      - ./valhalla/:/data/valhalla/
    environment:
      TILE_PATH: /data/valhalla/tiles.tar
    ports:
      - "8002:8002"
